# secrets — never commit
.env
.env.*
!.env.example

# macOS / editor noise
.DS_Store
.claude/settings.local.json

# dependencies & build caches (events-src, explainer)
node_modules/
.cache/
*.tsbuildinfo

# one-off recovery/migration dumps (see 90 System incident 2026-08-18)
*-recovery-*.json
