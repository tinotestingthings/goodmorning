# public/

Alles wat hier staat is met `staticFile("bestandsnaam")` op te vragen vanuit de
video: mp4's, afbeeldingen, geluid.

Zet je schermopname hier neer als `app-demo.mp4` en gebruik `<ScreenRecording>`
(zie `src/components/ScreenRecording.tsx`).
