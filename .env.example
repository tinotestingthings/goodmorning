# Copy to .env (gitignored). Real values live in macOS Keychain / 1Password.
# service_role is server-side ONLY: backups + capture-bridge sync. Never in app code.
SUPABASE_URL=https://bobltktjohhnoqhnxslf.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
# fine-grained PAT, scoped to tinotestingthings/goodmorning, Contents: read/write
GITHUB_PAT_GOODMORNING=
